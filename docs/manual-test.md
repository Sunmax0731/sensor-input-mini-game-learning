# 手動テスト

Codex側では手動テスト未実施です。

1. `npm test` を実行する。
2. `index.html` を開く。
3. `happy-path`、`missing-required`、`warning`、`mixed-batch` を順に選択する。
4. canvasと結果パネルが更新されることを確認する。
5. simulator、mock device、sample telemetry、Web dashboard、security/privacy の安全境界を必須とする。
6. release asset 3件を確認する。
